import React from 'react';

import HomeContainer from '../../containers/HomeContainers'

const Home = (props) => {
    return (
        <div>
            <HomeContainer />
        </div>
    );
};

export default Home;
